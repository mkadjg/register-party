package com.registerparty.repository;

import com.registerparty.model.StatusPerkawinan;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatusPerkawinanRepository extends JpaRepository<StatusPerkawinan, Integer> {
}
